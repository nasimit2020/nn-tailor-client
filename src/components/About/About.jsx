import React, { useContext } from 'react';
import { useLoaderData } from 'react-router-dom';

const About = () => {
    
    return (
        <div>
            Service Name
            
        </div>
    );
};

export default About;